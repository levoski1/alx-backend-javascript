/**
 * Creates a Set from an array.
 * @param {Array} anyArray - The array to convert into a Set.
 * @returns {Set} - A Set containing the unique elements from the array.
 */
export default function setFromArray(anyArray) {
    return new Set(anyArray);
}
